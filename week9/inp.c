#include <stdio.h>

int main()

{

    int a;

    int b;

    int neetigya, poddar;

    int l[9], c, ba[29];
    int i;
    a = b;
    if (a == b)
    {
        b = c;
    }
    else
    {
        a = c;
    }
    // a *b;
    // a++;

    while (a < 10)
    {
        c = a * 10;
    }
    // c = a * b;
    // a++;
    // }
    neetigya = a;
    c = a + b * c;
    c = a == b;

    b = a <= c;

    c = a * b;
}